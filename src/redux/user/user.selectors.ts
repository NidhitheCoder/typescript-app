export const GetUsers = (state: any): any => state["users"];

// export const GetUser = (state: any): any => {
//   console.log(state)
//   const users = state["users"].users;
//   return users.find((user: any) => user.id === 1);
// };
